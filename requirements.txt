# Framework de agentes
agno[google]

# LLM e Embeddings (Gemini)
google-generativeai

# HTTP client (async) para API de pedidos
httpx

# Validação de dados
pydantic>=2.0

# ORM / DB (dependência do agno.db.sqlite)
sqlalchemy

# Variáveis de ambiente
python-dotenv

# Testes
pytest
pytest-asyncio

# Linting e formatting
ruff
